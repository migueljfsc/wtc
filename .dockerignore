.git
bin/
dev/
demo/
testdata/
docs/
# NOTE: web/ must stay in the build context — its static/ tree is go:embed-ed
# into the binary; excluding it breaks the build.
*.db
*.db-wal
*.db-shm
.github/
deploy/
